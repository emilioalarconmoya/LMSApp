<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LMSApp.View.ContenedorUam"
             BindingContext="{Binding Main, Source={StaticResource Locator}}">
    <ContentPage.Content>
        <StackLayout
            BindingContext="{Binding ContendorUams}"
            Orientation="Horizontal">
            <WebView
                WidthRequest="1000"
                HeightRequest="1000"
                x:Name="contenedor"
                Source="{Binding RutaUam}">
            </WebView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>